#include <bits/stdc++.h>
using namespace std;
/*
b1: ta tạo 2 số từ các số của dãy 
sắp xép theo chiều tăng dần 
b2 lấy xen kẽ và chú ý khi đổi sang stoll nếu chuỗi rỗng mà chuyển sẽ bị lỗi 
*/
int main() {
    int t;
    cin >> t;

    while (t--) {
        int n;
        cin >> n;

        int a[n];

        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }

        sort(a, a + n);

        string s1 = "", s2 = "";

        for (int i = 0; i < n; i++) {
            if (i % 2 == 0)
                s1 += (a[i] + '0');
            else
                s2 += (a[i] + '0');
        }
		long long x1= 0, x2 = 0;
        if(!s1.empty()){
            x1 = stoll(s1);
        }
        if(!s2.empty()){
            x2 = stoll(s2);
        }

        cout << x1 + x2 << endl;
    }
}
